#include <stdio.h>

void main(void)
{
    int q_cavalos = 0, q_ferraduras = 0;
    
    printf("Digite a quantidade de cavalos: ");
    scanf("%d", &q_cavalos);
    
    printf("É necessário %d ferraduras para equipar todos os cavalos.", q_ferraduras = q_cavalos*4);
}
