#include <stdio.h>

void main(void)
{
    float peso_prato = 0, v_pagar = 0;
    
    printf("Digite o peso em Kg do seu prato: ");
    scanf("%f", &peso_prato);
    
    printf("O valor a ser pago é: %0.2f reais ", v_pagar = 12*peso_prato);
}
